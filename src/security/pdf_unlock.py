from pathlib import Path


def unlock_pdf(pdf_path: Path, password: str, output_path: Path) -> None:
    """Unlock a password-protected PDF into output_path using pikepdf."""
    raise NotImplementedError("PDF unlock not implemented until Phase 3")
