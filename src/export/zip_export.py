from pathlib import Path


def create_zip(markdown_files: list[tuple[str, str]], output_path: Path) -> None:
    """Bundle a list of (filename, markdown_content) tuples into a ZIP archive."""
    raise NotImplementedError("ZIP export not implemented until Phase 6")
